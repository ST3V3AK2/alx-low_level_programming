#include "3-function_like_macro.h"
#include "3-function_like_macro.h"
#include <stdio.h>

/**
 * main - Check the code
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;
	int r;

	r = ABS(-98) * 10;
	a = ABS(98) * 10;
	printf("%d\n", a);
	printf("%d\n", r);
	return (0);
}
